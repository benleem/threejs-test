import styles from './loading.module.css'

const Loading = () => {
    return(
        <div className={styles.assetsLoading}>
            <h1 className='assets-loading'>Loading...</h1>
        </div>
    );
};

export default Loading;
